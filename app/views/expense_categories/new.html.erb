<% content_for :title, "New Expense Category" %>

<div class="container mt-4">
  <h1 class="mb-4">New Expense Category</h1>

  <div class="card">
    <div class="card-body">
      <%= render "form", expense_category: @expense_category %>
    </div>
  </div>

  <br>

  <div class="mt-3">
    <%= link_to "Back to Expense Categories", expense_categories_path, class: "btn btn-secondary" %>
  </div>
</div>
